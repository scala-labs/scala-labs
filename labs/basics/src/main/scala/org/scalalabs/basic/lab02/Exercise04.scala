package org.scalalabs.basic.lab02

object Exercise04 {
	def firstElementInList(l:List[Any]):Any = {
		"Fix me"
	}

  def lastElementInList(l:List[Any]):Any = {
    "Fix me"
  }

  def nthElementInList(n:Int, l:List[Any]):Any = {
    "Fix me"
  }

	def concatLists(list1:List[Any], list2:List[Any]):List[Any] = {
		List("Fix me")
	}
 
	def elementExists(theList:List[String], elementToSearchFor:String):Boolean = {
		false
    }

 	def oddElements(listOfInts:List[Int]):List[Int] = {
 		List(-1)
    }
}
