<log>
  filename = "./logs/scalalabs.log"
  roll = "daily"  # Options: never, hourly, daily, sunday/monday/...
  level = "debug" # Options: fatal, critical, error, warning, info, debug, trace
  console = on
</log>

<scalalabs>
  version = "1.0-SNAPSHOT"
<scalalabs>